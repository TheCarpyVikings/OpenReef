# OpenReef

Private-beta Home Assistant add-on for the OpenReef controller dashboard.

Install the `openreef` Home Assistant custom integration first, then install and start this add-on from the same repository.
